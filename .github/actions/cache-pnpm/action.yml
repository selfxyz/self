name: Cache PNPM

description: Cache pnpm dependencies with consistent keys and paths.

inputs:
  path:
    description: Paths to cache
    required: false
    default: |
      ~/.pnpm-store
  lock-file:
    description: Path to pnpm-lock.yaml
    required: false
    default: pnpm-lock.yaml
  cache-version:
    description: Additional cache version segment
    required: false
    default: v1

outputs:
  cache-hit:
    description: Whether an exact match was found for the cache key
    value: ${{ steps.cache.outputs.cache-hit }}

runs:
  using: "composite"
  steps:
    - id: get-hash
      name: Hash lock file
      shell: bash
      run: |
        if [ -f "${{ inputs.lock-file }}" ]; then
          echo "hash=$(shasum -a 256 "${{ inputs.lock-file }}" | awk '{ print $1 }')" >> $GITHUB_OUTPUT
        else
          echo "::warning::Lock file '${{ inputs.lock-file }}' not found."
          echo "hash=no-lock-file" >> $GITHUB_OUTPUT
        fi
    - id: cache
      name: Cache pnpm dependencies
      uses: actions/cache@v4
      with:
        path: ${{ inputs.path }}
        key: ${{ runner.os }}-pnpm-${{ inputs.cache-version }}-${{ steps.get-hash.outputs.hash }}
        restore-keys: |
          ${{ runner.os }}-pnpm-${{ inputs.cache-version }}-
          ${{ runner.os }}-pnpm-

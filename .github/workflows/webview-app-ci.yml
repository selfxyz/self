name: WebView App CI

permissions:
  contents: read

on:
  pull_request:
    paths:
      - "common/**"
      - "packages/mobile-sdk-alpha/**"
      - "packages/webview-app/**"
      - "packages/webview-bridge/**"
      - ".github/workflows/webview-app-ci.yml"
      - ".github/actions/**"
  push:
    branches: [dev, staging, main]
    paths:
      - "common/**"
      - "packages/mobile-sdk-alpha/**"
      - "packages/webview-app/**"
      - "packages/webview-bridge/**"
      - ".github/workflows/webview-app-ci.yml"
      - ".github/actions/**"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - name: Install dependencies
        uses: ./.github/actions/pnpm-install
      - name: Build common
        run: pnpm workspace @selfxyz/common build
      - name: Build mobile-sdk-alpha
        run: pnpm workspace @selfxyz/mobile-sdk-alpha build
      - name: Build webview-bridge
        run: pnpm workspace @selfxyz/webview-bridge build
      - name: Build webview-app
        run: pnpm workspace @selfxyz/webview-app build

  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - name: Install dependencies
        uses: ./.github/actions/pnpm-install
      - name: Build common
        run: pnpm workspace @selfxyz/common build
      - name: Build mobile-sdk-alpha
        run: pnpm workspace @selfxyz/mobile-sdk-alpha build
      - name: Build webview-bridge
        run: pnpm workspace @selfxyz/webview-bridge build
      - name: Run linter
        run: pnpm workspace @selfxyz/webview-app lint

  format:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - name: Install dependencies
        uses: ./.github/actions/pnpm-install
      - name: Check Prettier formatting
        run: pnpm workspace @selfxyz/webview-app fmt

  types:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - name: Install dependencies
        uses: ./.github/actions/pnpm-install
      - name: Build common
        run: pnpm workspace @selfxyz/common build
      - name: Build mobile-sdk-alpha
        run: pnpm workspace @selfxyz/mobile-sdk-alpha build
      - name: Build webview-bridge
        run: pnpm workspace @selfxyz/webview-bridge build
      - name: Typecheck
        run: pnpm workspace @selfxyz/webview-app types

# AGENTS Instructions

## Recommended Workflow

```bash
# Fix formatting and linting issues
yarn nice

# Lint source files
yarn lint

# Check types
yarn types

# Run tests
yarn test
```

## Running the App

- `yarn ios` – run the app on the iOS simulator
- `yarn android` – run the app on the Android emulator
- `yarn web` – start the web version

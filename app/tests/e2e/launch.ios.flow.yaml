# Launch smoke test: cold-start the app and prove it boots into the WebView-In-App
# (WIA) onboarding without error. The launch readiness contract lives in the shared
# subflow so a future onboarding change is a one-file fix.
appId: com.warroom.proofofpassport
---
- clearState
- clearKeychain
- runFlow: subflows/launch-webview-host.ios.yaml

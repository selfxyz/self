# Launch smoke test: cold-start the app and prove it boots into the legacy native
# onboarding without error. WIA has not cut over yet, so this must match what ships
# to the stores. The launch readiness contract lives in the shared subflow so a
# future onboarding change is a one-file fix.
appId: com.proofofpassportapp
---
- clearState
- clearKeychain
- runFlow: subflows/launch-onboarding.android.yaml
